<div class="max-w-sm h-screen flex justify-center items-center">
    <div role="alert">
        <h1>{{ heading }}</h1>
        <p class="my-6">{{ body }}</p>
        <a href="{{ url() }}">
            <span aria-hidden="true">&larr;</span> {{ 'Go home'|t }}
        </a>
    </div>
</div>
