{% set data = {
    title: null,
    url: null,
    class: 'w-full h-full object-cover',
    attributes: {
        muted: true,
        controls: true,
    }
}|merge(_context) %}

<video {{ attr(data.attributes) }} class="{{ data.class }}" aria-label="{{ data.title ?? 'Video'|t }}">
    <source src="{{ data.url }}" type="video/mp4">
    {{ 'Your browser does not support the video tag.'|t }}
</video>
