<a href="{{ entry.url }}" class="flex flex-col justify-between p-12 bg-gray-100">
    <div class="mb-2">{{ entry.type.name }}</div>
    <div class="text-sm text-gray-500">
        Create a template in <code class="text-xs">_includes/builder/blocks/showcase/{{ entry.type|kebab }}</code> to customise this preview.
    </div>
    <div class="mt-24 text-xl font-bold">{{ entry.title }}</div>
</a>
