<a href="{{ entry.url }}" class="flex flex-col justify-between h-96 p-12 bg-gray-100">
    <div class="mb-2">{{ entry.type.name }}</div>
    <div class="text-sm text-gray-500">
        Create a template in <code class="text-xs">_includes/builder/blocks/content-cards/{{ entry.type|kebab }}</code> to customise this card.
    </div>
    <div class="mt-24 text-xl font-bold">{{ entry.title }}</div>
</a>
