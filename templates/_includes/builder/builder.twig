{% set context = context ?? null %}

{% for block in blocks %}
    {# <code class="text-xs text-blue-500">
        {{ '_includes/builder/blocks/' ~ context ~ '/' ~ block.type|kebab }}<br>
        {{ '_includes/builder/blocks/' ~ block.type|kebab }}
    </code> #}

    {% include [
        '_includes/builder/blocks/' ~ context ~ '/' ~ block.type|kebab,
        '_includes/builder/blocks/' ~ block.type|kebab,
    ] with {
        block: block,
        key: key ?? block.type
    } only %}
{% endfor %}
